# envexec - examples / docker / base

This example demonstrates how to build your docker image based on
envexec image using FROM directive.

## Run

```bash
docker build -t envexec-example-base .
docker run --rm envexec-example-base
```
